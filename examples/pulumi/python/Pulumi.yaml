name: motherduck-pulumi-example
description: A minimal Pulumi Python program using the MotherDuck Terraform provider
runtime:
  name: python
  options:
    virtualenv: .venv
packages:
  motherduck:
    source: terraform-provider
    version: 1.4.0
    parameters:
      - ./bin/terraform-provider-motherduck
