---
page_title: "motherduck_share_grants Data Source - motherduck"
subcategory: "Access control"
description: |-
  Lists the users, roles, and organization-wide grants that can read one MotherDuck share.
---

# motherduck_share_grants (Data Source)

Reports who can read one share, so an access review can run against the same
configuration that created the grants. Each row is one grant:
[motherduck_share_grant](../resources/share_grant.md) creates the `user` and
`role` rows, and the share's own `access` mode produces the rest.

| `grantee_type` | `grantee_name` | Comes from |
| --- | --- | --- |
| `user` | the username | `GRANT READ ON SHARE ... TO USER` |
| `role` | the role name | `GRANT READ ON SHARE ... TO ROLE` |
| `organization` | `ENTIRE_ORGANIZATION` | a share created with `access = "organization"` |
| `domain` | `ALL_USERS` | a share created with `access = "unrestricted"` |

A share with `access = "organization"` or `access = "unrestricted"` reports one
of those whole-audience rows instead of individual grants. Only a restricted
share carries `user` and `role` grants.

Read it with the share owner's credentials. An organization admin can also read
grants on shares the organization owns. A caller who is neither sees a
share-not-found error, because share lookup happens before the grant read.

This data source inspects existing objects without taking lifecycle ownership.
Use it after the referenced objects exist and with credentials that can read
them. See [authentication](../guides/authentication.md).

## Example Usage

```terraform
data "motherduck_share_grants" "analytics" {
  share_name = "analytics_share"
}
```

Terraform reads data sources during plan, so the grant list refreshes on every
plan and can back a drift alert or a reviewed audit output:

```terraform
output "analytics_share_readers" {
  value = [
    for grant in data.motherduck_share_grants.analytics.rows :
    "${grant.grantee_type}:${grant.grantee_name}"
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `share_name` (String) MotherDuck share name.

### Read-Only

- `rows` (Attributes List) Typed catalog rows for stable MotherDuck metadata. `rows_json` remains available for raw server columns. (see [below for nested schema](#nestedatt--rows))
- `rows_json` (String, Sensitive) Raw MotherDuck catalog rows encoded as JSON. Sensitive because catalog rows can include share URLs and account metadata.

<a id="nestedatt--rows"></a>
### Nested Schema for `rows`

Read-Only:

- `granted_at` (String) Grant creation timestamp.
- `grantee_name` (String) Role name, username, or the ENTIRE_ORGANIZATION or ALL_USERS keyword reported for an organization-wide or public share.
- `grantee_type` (String) Grantee type: role, user, organization, or domain.
- `privilege` (String) Granted share privilege.
- `share_owner` (String) Account that owns the share.
