---
page_title: "motherduck_database_snapshots Data Source - motherduck"
subcategory: ""
description: |-
  Reads MotherDuck database snapshot metadata from public SQL catalog tables.
---

# motherduck_database_snapshots (Data Source)

Reads MotherDuck database snapshot metadata from public SQL catalog tables.

`rows_json` mirrors `MD_INFORMATION_SCHEMA.DATABASE_SNAPSHOTS`. When `database_name` is set, MotherDuck can still return more than one row for that database, including retained snapshots whose `snapshot_name` is `null`. Filter the decoded JSON in Terraform or downstream tooling when you need only a specific named snapshot.

## Example Usage

```terraform
data "motherduck_database_snapshots" "analytics" {
  database_name = "analytics"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `database_name` (String) MotherDuck database name filter.
- `limit` (Number) Maximum number of rows to return when the underlying MotherDuck catalog function supports limits.
- `offset` (Number) Number of rows to skip when the underlying MotherDuck catalog function supports offsets.

### Read-Only

- `rows` (Attributes List) Typed catalog rows for stable MotherDuck metadata. `rows_json` remains available for raw server columns. (see [below for nested schema](#nestedatt--rows))
- `rows_json` (String, Sensitive) Raw MotherDuck catalog rows encoded as JSON. Sensitive because catalog rows can include share URLs and account metadata.

<a id="nestedatt--rows"></a>
### Nested Schema for `rows`

Read-Only:

- `created_ts` (String) Snapshot creation timestamp.
- `database_name` (String) Database that owns the snapshot.
- `snapshot_id` (String) MotherDuck snapshot ID.
- `snapshot_name` (String) Snapshot name when the snapshot is named.
