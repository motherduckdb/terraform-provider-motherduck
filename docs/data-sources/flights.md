---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "motherduck_flights Data Source - motherduck"
subcategory: ""
description: |-
  Lists MotherDuck Flights available to the current account. Callers with organization-wide Flight visibility can restrict results to their own Flights.
---

# motherduck_flights (Data Source)

Lists MotherDuck Flights available to the current account. Callers with organization-wide Flight visibility can restrict results to their own Flights.

## Example Usage

```terraform
data "motherduck_flights" "recent" {
  limit      = 10
  offset     = 0
  owner_only = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `limit` (Number) Maximum number of rows to return when the underlying MotherDuck catalog function supports limits.
- `offset` (Number) Number of rows to skip when the underlying MotherDuck catalog function supports offsets.
- `owner_only` (Boolean) Whether to restrict Flight listings to Flights owned by the current user. Ignored for callers who can only view their own Flights.

### Read-Only

- `rows` (Attributes List) Typed catalog rows for stable MotherDuck metadata. `rows_json` remains available for raw server columns. (see [below for nested schema](#nestedatt--rows))
- `rows_json` (String, Sensitive) Raw MotherDuck catalog rows encoded as JSON. Sensitive because catalog rows can include share URLs and account metadata.

<a id="nestedatt--rows"></a>
### Nested Schema for `rows`

Read-Only:

- `created_at` (String) Flight creation timestamp.
- `current_version` (String) Current Flight version.
- `flight_id` (String) Flight ID.
- `flight_name` (String) Flight name.
- `owner_name` (String) Flight owner name when exposed to the caller.
- `schedule_cron` (String) Flight cron schedule.
- `schedule_status` (String) Flight schedule status.
- `status` (String) Flight status.
- `updated_at` (String) Flight update timestamp.
